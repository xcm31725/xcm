package com.tcp;

import java.io.IOException;

public class ConnectSocket extends Thread {
	/*public void run() {//将多用户链接过程在run()方法中实现
		while(true) {
			try {
				//socket = sernevr.accept();
			}catch(IOException e) {
				//jL1.append("用户链接服务器出错\n");
			}
			if(socket != null) {
				//创建收发信息线程对象
				ReadMEssageThread randThread = new ReadMEssageThread(socket);
				//激活线程
				randThread.start();
			}
			
		}
		
	}*/
}
